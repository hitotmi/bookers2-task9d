<% if user_signed_in? %>
  <div class="search_form">
    <%= form_with url: tagsearches_search_path, local: true,metod: :get do |f| %>
      <%= f.text_field :word %>
      <%= f.submit "タグ検索",class: "btn btn-info">
    <% end %>
  </div>
<% end %>